<html>

<head>


</head>

<body>
    Edit Your Coin
    <a href="/edit/{{id}}">Edit</a>
    <a href="/">view all</a>
    <!-- {{#Coin}} -->
    <a href="/{{id}}">view</a>
    <div class="coin_edit">
        <form action="/edit/{{id}}" method='post'>
            <input type="text" name="name" placeholder="{{name}}" value="{{name}}">
            <input type="number" name="year" placeholder="{{year}}" value="{{year}}">
            <!-- <select name="year">
                            <option value="1800's">1800's</option>
                            <option value="1900">1900-1950</option>
                            <option value="1950">1950-2000</option>
                            <option value="2000">2000+</option>
                        </select> -->
            {{#location}}
            <input type="text" name="state" placeholder={{state}} value="{{state}}">
            <input type="text" name="country" placeholder={{country}} value="{{country}}"> {{/location}}
            <!-- {{#location}} -->
            From: {{state}}, {{country}}
            <!-- {{/location}} -->
            Details:
            <textarea name="description" placeholder="{{#description}}{{.}}{{/description}}">{{#description}}{{.}}{{/description}}</textarea>
            <button type="submit">save changes</button>
    </div>
    <!-- {{/Coin}} -->

</body>

</html>